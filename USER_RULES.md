# User Rules

1. **Explicit Confirmation Required**: Before applying any changes to the code (editing files, refactoring logic, fixing bugs), you MUST explain the plan and ask for the user's explicit permission ("Đồng ý" or "OK"). Do NOT proceed with `replace_file_content` or `multi_replace_file_content` until this confirmation is received.
