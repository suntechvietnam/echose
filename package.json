{
  "name": "ytbflow",
  "version": "0.1.0",
  "description": "YTBFlow Desktop App",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run copy:effects && vite build",
    "preview": "vite preview",
    "tauri": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri",
    "tauri:dev": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri dev",
    "tauri:build": "npm run auto-download:ffmpeg && PATH=\"$HOME/.cargo/bin:$PATH\" tauri build && cd src-tauri && ./scripts/copy-resources.sh target/release/bundle/macos/YTBFlow.app resources/",
    "tauri:build:windows": "npm run auto-download:ffmpeg && npx tauri build --bundles msi,nsis"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0",
    "vue": "^3.4.0",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
